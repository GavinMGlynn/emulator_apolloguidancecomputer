// Headers this toolchain does not pull in transitively the way the upstream
// build environment does. Forced in with -include so ext/ stays untouched.
#include <iostream>
#include <algorithm>
#include <cmath>
